import { FunctionComponent, memo } from 'react';

export const Home: FunctionComponent = memo(() => {
	return (
		<div id="home" className="flex-grow h-screen flex flex-col p-16 relative">
			hello worlss
		</div>
	);
});
